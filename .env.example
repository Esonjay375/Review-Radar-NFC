# Server-only secret. Never expose this in the browser.
# Get one at https://console.cloud.google.com (enable the "Places API", then create an API key)
GOOGLE_PLACES_API_KEY=your_google_places_api_key_here
